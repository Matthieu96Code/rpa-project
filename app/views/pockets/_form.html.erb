<%= form_with(model: pocket) do |form| %>
  <% if pocket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pocket.errors.count, "error") %> prohibited this pocket from being saved:</h2>

      <ul>
        <% pocket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.text_field :author_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
